# 工作記錄

## 2026-09-14

### 本次完成

- 建立醫療法規標準資料庫查詢器的系統 Spec。
- 盤點 `D:\6.查检相关法律法规`，確認約 2,725 個檔案，包含 PDF、DOCX、DOC、XLSX、PPTX 等格式。
- 建立 Node.js 索引 MVP，成功建立 2,705 筆索引。
- 建立依檔名與資料夾的法域、文件性質及醫療主題分類規則。
- 建立分類總表、分類統計及待人工確認清單。
- 加入 DOCX／XLSX／PPTX 的 Office XML 文字擷取。
- 嘗試 PDF 文字解析；由於全量 PDF 解析耗時過長，目前保留為後續分批處理或 OCR 工作。
- 將分類結果調整為「已分類候選」與「待確認」，避免因法域未知而讓大量已有主題分類的文件全部被視為未分類。
- 建立本地查詢 API 與 Web 介面。
- 重新設計查詢介面，改為先顯示醫療主題與文件類型分類，再進一步搜尋。
- 啟動並驗證 `http://localhost:3000`，API 與分類查詢正常。

### 目前數據

- 掃描檔案：2,725
- 建立索引：2,705
- 已分類候選：2,703
- 待確認：2
- 已讀取 Office XML 文字：299

### 尚未完成

- PDF 內文全面抽取與 OCR。
- 人工分類審核流程。
- 既有資料庫串接。
- 正式搜尋引擎、SSO、權限及稽核功能。

### 下次建議從這裡開始

1. 先處理 `data/classification/review-queue.csv` 的 2 筆檔名無法判斷文件。
2. 評估安裝或接入可靠的 PDF 文字擷取與 OCR 工具。
3. 依實際資料庫 schema 設計同步程式。
4. 為文件分類建立人工審核頁面。

### 執行方式

```powershell
npm.cmd run index -- "D:\6.查检相关法律法规"
node src/report.mjs
npm.cmd start
```

## 2026-09-14（收工記錄）

### 本次完成

- 依使用者回饋重新設計搜尋介面，不再以單一搜尋框作為主要入口。
- 改為三層瀏覽流程：第一層醫療主題／文件類型，第二層地區／省份／年份，第三層文件結果。
- 在索引建立器加入 `region`、`year`、`years` 欄位。
- 依檔案路徑、檔名及標準號辨識地區與年份。
- 重新建立 2,705 筆索引及分類報告。
- 建立 `/api/facets` 的連動分類統計。
- 更新本地 Web 介面，加入分類卡片、地區與年份標籤、篩選連動、分頁及清除條件。

### 驗證結果

- 本地服務：`http://localhost:3000` 正常啟動。
- 首頁 HTML：正常回應。
- 分類 API：正常回應。
- 「感染預防與控制」：296 筆。
- 前端 JavaScript 語法：檢查通過。

### 下次建議

1. 實際操作新版介面，確認分類卡片與地區／年份層級是否符合院內使用習慣。
2. 若分類仍太多，可再增加「法規層級」或「科室」作為另一個視覺入口。
3. 後續處理 PDF 內文抽取與 OCR。
4. 再進行人工分類審核及既有資料庫串接。

### 交接觸發規則

當使用者輸入「開工」時：

1. 先讀取 `handoff.md`。
2. 再讀取 `worklog.md`。
3. 整理上一次的工作狀況、未完成事項與接下來的工作待辦。
4. 再開始處理新的工作。

當使用者說「收工」或「今天先這樣」時：

1. 將本次對話完成的工作、測試結果與未完成事項寫入 `worklog.md`。
2. 更新 `handoff.md` 的目前狀態、下一步與注意事項。
3. 回覆使用者本次交接摘要及下次建議起點。

## 2026-09-15

### 本次完成

- 依交接先檢查待確認清單，確認只有 2 筆 PDF：`1733125810893_50492.pdf`、`1743499725091.pdf`。
- 確認兩筆原始檔不在 `D:\6.查检相关法律法规`，也不在目前工作區，因此未對缺少原文的文件進行臆測分類。
- 重新執行 `node src/report.mjs`，修正過期的 `classification-summary.json`；目前與 `review-queue.csv` 一致。

### 驗證結果

- 摘要：2,705 筆總索引、2,703 筆候選、2 筆待確認。
- `node --check src/server.mjs` 通過。
- `node --check src/indexer.mjs` 通過。
- 隔離埠 3317：首頁 HTTP 200；`/api/health` indexed 2705；`/api/search?q=感染` 回傳 190 筆。
- 3000 埠已有其他服務，該服務回應 404，需避免用它判斷本專案狀態。

### 尚未完成／阻塞

- 兩筆待確認 PDF 缺少原始檔，尚未完成 PDF 內容抽取、OCR 或人工分類。
- 尚未建立人工分類審核介面。

### 下次建議

1. 取得 PDF 或新的來源路徑後重建索引。
2. 若原文仍不可得，先建立審核介面及分類編輯資料格式。

### 今日部署

- 初始化 Git repository 並推送至 `https://github.com/emmyyang1972/0915check.git`。
- 補上 `api/index.mjs` 與 `vercel.json`，讓 Vercel 能提供查詢 API。
- 修正 Vercel API 404 路由問題，完成 production 部署至 Vercel 專案 `0915check`。
- 線上網址：`https://0915check.vercel.app`。
- 線上 smoke test 通過：首頁 200、健康檢查 2,705 筆、facets 2,705 筆、感染搜尋 190 筆。
- 今日 Git 提交：`b3c3886`、`1105fbb`、`6dc1778`、`58a51fc`。

### 收工狀態

- 工作區乾淨，`main` 已追蹤 `origin/main`。
- 下次開工先處理缺少原始檔的 2 筆待確認 PDF，或開始建立人工分類審核介面。
